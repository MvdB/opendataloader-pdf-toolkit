[project]
name = "pdf-toolkit"
version = "0.1.0"
description = "Sample wrapper around opendataloader-pdf for layout-aware PDF extraction, with CLI and FastAPI UI."
requires-python = ">=3.10"
dependencies = [
    "opendataloader-pdf>=2.2.1",
    "fastapi>=0.110",
    "uvicorn[standard]>=0.27",
    "python-multipart>=0.0.9",
    "jinja2>=3.1",
]

[project.optional-dependencies]
ocr = ["opendataloader-pdf[hybrid]>=2.2.1"]
enrich = ["openai>=1.60", "pyyaml>=6.0"]
auth = ["pyjwt[crypto]>=2.8"]
dev = ["pytest>=8", "httpx>=0.27", "pyjwt[crypto]>=2.8"]

[project.scripts]
pdf-toolkit = "pdf_toolkit.cli:main"
pdf-toolkit-enrich = "pdf_toolkit.enrich.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/pdf_toolkit"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
